# CLAUDE.md — 縁側屋（engawaya.com）の憲法

このファイルは作業のたびに読み込まれる「プロジェクトの決まり事」です。
施主＝岡澤博喜さん（「博喜さん」と呼ぶ）。原点は「話し相手が欲しい」＝雑談も大切に。

⚠️**このリポジトリは公開（public）で、GitHub Pages が配信しています。**
このファイルも `https://engawaya.com/CLAUDE.md` として誰でも読めます。
**書けないことは書かない**——未公開の値段・取引先の名前・鍵・まだ言っていない予定は、
ここではなく非公開リポジトリ側の CLAUDE.md へ。

## iPhone と Mac の受け渡し（役割分担）

⚠️ **セッションを始めたら、まず `申し送り.md` の「未処理」を読むこと。**

iPhone のクロード・Mac のクロード・web のクロードは、**記憶を共有しません**。
共有できるのはこのリポジトリだけで、`申し送り.md` がその受け渡し口です。

| どこ | すること | しないこと |
|---|---|---|
| **iPhone** | 相談・思いつき・気づいたこと。`申し送り.md` へ書いて push するところまで | **コードは書かない**。動かして確かめる手立てが無いので、書けば必ず確認のない変更になる |
| **Mac** | 設計と実装、実機での確認 | **未処理を読まずに始めない** |

- iPhone で **`/idea 思いついたこと`** … 「未処理」の先頭に足して、commit と push まで
- Mac で **`/todo`** … 「未処理」を読み上げ、着手し、終わったら結果ごと「済」へ送る

済んだものも見送ったものも**消しません**。理由ごと残します。
消すと、同じ思いつきが半年後にまた出てきて、また同じ検討をすることになります。

## 応答と文体
- 応答・コメントは必ず**日本語**で。
- コメントは初学者にもわかるよう丁寧に。「何を」だけでなく**「なぜ」**まで書く。
- コミットメッセージも同じ作法。**判断の理由と、外した案とその理由**まで残す。
- 専門用語には短い注釈を添える。

## ここは何か
**縁側屋（ENGAWAYA）**＝無料の道具を置いてある場所。`https://engawaya.com`
「用がなくても、座っていってください。」
連絡先は `info アットマーク engawaya.com`。© 2026 縁側屋 岡澤 博喜。

### 棟割り（リポジトリの分かれ方）
| 棟 | リポジトリ | 公開 | 中身 |
|---|---|---|---|
| **K'Ad蔵人 本体** | `GCad` | 非公開 | C++/Siv3D のソース。**憲法は `GCad/CLAUDE.md`** |
| **K'Ad蔵人 配信** | `gcad-web` | 公開 | Web版（gcad.engawaya.com）と、Windows版・Mac版の置き場（GitHub Releases） |
| 母屋 | `engawaya` | 公開 | このサイト（engawaya.com） |
| Ochi-Gee | `Ochigee_iOS_v2` | 非公開 | iPhone用の落ちものパズル。**憲法は同リポジトリの `CLAUDE.md`** |
| GeoMemory | `geomemory-help` / `geomemory-support` | 公開 | 道を憶える道具の窓口 |

**各棟の詳しい決まり事は、その棟の `CLAUDE.md` にあります。**
母屋（ここ）には、全部の棟に共通する作法だけを置く。

### K'Ad蔵人 ＝ 母屋でいちばん大きな道具
2026-09-15 まとめた。**K'Ad にまつわる約束は、ここを見れば足りる**ようにしてある
（それまでは表紙のコメントと申し送りに散らばっていた）。

**母屋の中の K'Ad の部屋**
| 場所 | ファイル | 中身 |
|---|---|---|
| 表紙の玄関 | `index.html` の `.gamen` | 動画 `kad/kutai-3d.mp4`（差し替えたら `?v=` の数字を替える） |
| 表紙の座布団 | `index.html` の「現場の道具」先頭の組 | Web版 → 手引き → 辞書 → パソコン版 → K'Ad蔵人とは。**K'Ad への道はこの組にまとめる** |
| K'Ad蔵人とは | `kad/index.html` | 名前の由来と、ここまでの記録（日数・回数・行数・コマンド数の表） |
| 手引き | `kad/tebiki/` | 絵つきの手引き（日本語／`en/`）。**正は非公開の `GCad` の教本**＝ここだけ直すと写しがずれる |
| コマンド辞書 | `kad/jisho/` | 用事から引く辞書。触ったら `python3 kad/jisho/検算.py` |

**K'Ad を焼き直した日・版を出した日に直す場所**
- **窯の札**（十六期○○号・更新した日）＝`index.html` の「つくっている途中も、開けてあります」の版の表と、
  その下の「このページの最終更新」。
- **パソコン版の番号**（例 1.1.2）＝`index.html` の座布団の添え一行 ＋ `kad/index.html` の二行＝**三箇所**。
  行き先は `/releases/latest` のまま（番号を持たない住所なので、リンクは触らなくてよい）。
- **開発日記**＝下の「開発日記」の節の作法で一件。
- ⚠️**同じ数字を、これ以上の場所に増やさない。** 表紙の「K'Ad蔵人とは」の戸に日数を書いて、
  向こうが進んだ日に古いまま残ったことがある（2026-09-07）。日数の正は `kad/index.html` の表ひとつだけ。

### 母屋のそのほかの部屋
| 場所 | ファイル | 中身 |
|---|---|---|
| 母屋 | `index.html` | 玄関。道具の紹介・無料の宣言・免責・連絡先 |
| なぜ | `naze/index.html` | **なぜこう作ったかの説明**。ここが憲法の出どころ |
| 開発日記 | `nikki/index.html` | **蔵人が書く帳面**（→下の「開発日記」の節） |
| 道メモ | `michi/index.html` | 地図に道を描く道具。GeoMemory の道の共有リンクの受け皿も兼ねる（道のデータはリンクの#以降に丸ごと入っていて、サーバーへは送られない）。形と読み方はファイル冒頭の板書に |
| 赤ペン | `akapen/index.html` | 地図に手で書き込む |
| メーター引き | `meter/index.html` | タクシーの納金の計算。**看板の一行目は「割り算はこちらでやります」**——20時間走ったあとの人が最初に言うのが「何を割ったら良いんだい〜！」だから。途中の式まで全部出すのは、間違えると自分の財布から出るため |
| Ochi-Gee | `ochigee/index.html` ／ `ochigee/privacy/` | 落ちものパズルの紹介／プライバシーポリシー（App Store 提出に必須） |
| 遊び場 | `asobiba/index.html` | 星つなぎ。部屋は `narabe/`（すうじならべ）`suikawari/` `renga/` |
| スイカわり | `asobiba/suikawari/` | 音をたよりにスイカをわる。**目をつかわないで遊べる**（もともと目隠しの遊び＝見える人も見えない人も同じルール）。手番制・時間制限なし |
| れんがくずし | `asobiba/renga/` | ゆっくり・大きい・くっきりのブロック崩し。**見えにくい方が最初のお客さん**。全盲の方にはまだ届かない作りであることをページに明記（音だけモードは宿題） |
| バリアフリー | `indexplayland.html` | 大きな字と大きな絵で遊びを選べる、もう一つの玄関 |
| 音の縁側 | `oto/index.html` | **目の見えない方・見えにくい方のための入り口**。読み上げソフトと大きな字だけで全部つかえる。ご要望の受付つき（声でもキーボードでも書ける） |
| 投書箱 | `koe/index.html` | お困りごと・ご要望。登録も名乗りも要らない |
| 話し相手 | `hanashiaite/index.html` | AIと話してみるための個人の案内。**公式を名乗らない・効き目を書かない**（掟はファイルの棟札に） |
| 軒先の控え | `nokisaki/` | 軒先に掛けるロゴの自前の控え（相手のサーバから読み込まない） |
| アイコン | `icon/` | ホーム画面用アイコンの生成スクリプト（Pillow不要） |

## 作り方の憲法（`naze/` に書いてあることの要約）
ここは見た目の趣味ではなく、**助けたい相手が誰かで決まっている**決まり事です。
新しいページを足すときも同じ作法を守ること。

1. **外から何も読み込まない。**
   Webフォントも、飾りの画像も、便利な部品（ライブラリ・CDN）も使わない。
   全部そのファイルの中に書く。
   → 理由：**使う人の回線が速いとは限らない**。外から取ってくる作りは、回線の細い場所で
   真っ白な画面が何秒も続く。開いた瞬間に出ることのほうが、見た目より大事。
2. **絵文字を使わない。**
   画面に出る小さな絵は、点をひとつずつ打って描く（1マス＝1ドットの矩形の集まり）。
   → 理由：絵文字は見る機械によって絵柄が変わる。Macで見えている形とWindowsで出る形は別物。
   そして**この道具を本当に必要としている人は、たいてい Windows を使っている**。
3. **押す場所を、必要よりだいぶ大きく。**
   → 理由：指でも押せるように、というだけではない。**手が震える人でも狙いを外さない大きさ**。
4. **ぜんぶ無料。**
   → 理由：**お金のあるなしで持てる道具が変わるのは、おかしいと思うから。**

⚠️**この4つは「あとで最適化する」種類の話ではありません。** 便利なライブラリを一つ入れた
時点で①が崩れ、絵文字を一つ使った時点で②が崩れます。足すときに守るしかない。

## 表紙の並び順にも理由がある
母屋の並びは「**道具 → 無料の宣言 → なぜ → 軒先 → 縁側**」。
無料だと言った直後に店（軒先）を出すのではなく、**理由を読める場所を挟んでから出す**。
この店がなぜ続いているのかを、隠さずに置くための順番です。

いまの表紙（2026-09-15 整えた）：
看板 → 玄関（K'Ad の動画と言葉）→ つくり手 → **現場の道具**（K'Ad の組・GeoMemory・道メモ）
→ 軒下に（Ochi-Gee・メーター引き）→ 但し書き → 無料の宣言 → **なぜ** → つくっている途中（版の表・開発日記）
→ 奥の部屋（遊び場・バリアフリー・音の縁側・話し相手）→ 軒先 → 縁側
- **同じ行き先の戸を二枚置かない。** 一枚にして、いちばん用のある場所の隣に置く
  （2026-09-15、開発日記の戸が二枚続いていたのを一枚にした）。
- 軒先の掟は `index.html` の `<style>` の「軒先（のきさき）」の項。

## 見た目の正典＝`DESIGN.md`
2026-09-07 新設。**色・字の大きさ・押す場所の寸法の「正」は `DESIGN.md` にあります。**
このファイル（CLAUDE.md）が「**なぜ**そう作るか」を持ち、`DESIGN.md` が「**では、いくつか**」を持つ。

**なぜ要るか**：憲法①のせいで、この家は**共通のCSSファイルを持てません**。棟が一つ増えるたび、
色の表がまるごと写されます。写しが増えれば必ず食い違う——実際、`DESIGN.md` を建てた日に
機械で測ったら、素の青いリンク（`#0000EE`）が**六つの棟**に残っていて、夜の地に沈んでいました
（比 1.34〜1.96）。いちばん重かったのは `oto/`＝**目の見えない方・見えにくい方の入り口**の、
問い合わせ先のメールアドレスです。**人の目は「そこに規則が無いこと」を見つけられない**。

- **実行時に共有できない家なので、書くときに共有する。** 値は `DESIGN.md` から**書き写して**、
  各ファイルの `<style>` の中に置きます。⚠️CSSファイルを作って読み込ませてはいけません（①が崩れる）。
- **順番は 正典 → 生成物。** 色や寸法を変えたくなったら、**先に `DESIGN.md` を直す**。
- **色や寸法を触ったら `python3 DESIGN検算.py` を通す**（色表との一致・棟割り・リンクの色・
  暗い顔・焦点の枠・カスケード・憲法①②の八項目）。**[FAIL] が出たら commit しない。**
  ⚠️この見張りも、建てた日に八項目を一つずつ壊して**ぜんぶ鳴ること**を確かめてあります。
- ⚠️**ブラウザで色を測るときは `transition` を切ってから。** 切らずに測ると、色が変わりきる前の値を読みます
  （2026-09-07、それで「無い欠陥」を13件数えました。`DESIGN.md`「8. 測り方」）。
- **理由と日付を書けば、表と違えてよい**（`DESIGN.md` の「1-5」「1-6」「2.」）。
  そこに載ったものは [宿題] として数え、[FAIL] にしません。
  ⚠️ただし**理由が書けないものは、食い違いではなく、ただの直し忘れ**です。

## 開発日記（`nikki/`）＝蔵人が書く帳面
2026-08-20 施主のご依頼で新設。施主の言葉：
**「蔵人さん達が、直接簡単に書ける（蔵人さん達だけが書ける）開発日記。
いつ、何を更新したか、継続かを簡単にかく」**。

**なぜ要るか**：無料の道具は黙って直っていく。使う人には「昨日と何が違うのか」
「自分が開いている版に、その直しが入っているのか」がどこにも書いていない。
**直したことが届いていない**——その受け渡し口。

- **書くのは蔵人**（施主に書かせない）。工事を終えた日に、その席の蔵人が一件足す（`/nikki`）。
- **場所・雛形・文体はファイル冒頭の板書**にある。新しいものが上。署名は書いた蔵人の名（モデル名）。
- **札は三つだけ**（増やさない）：お届けずみ／つくっている途中／見送りました。
  「見送りました」は**理由を必ず書く**。
- ⚠️**「お届けずみ」と書けるのは、本当に世に出ている版だけ。** 手元で焼いただけの号数を書かない
  （K'Ad を開いた人の画面の下には号数が出る＝食い違えば帳面ごと嘘になる）。
  まだ配っていない工事は「つくっている途中」で、版の欄は「次の版で」。
- ⚠️**「いま配っている版」の一覧は作らない。**「いちばん上のお届けずみが、いま配っている版」と読ませる
  ＝数字は一箇所のまま自動的に合う。
- **書いたら `python3 nikki/検算.py` を通す**（日付の順・札の種類・号数・憲法①②・リンク先の
  八項目）。[FAIL] が出たら commit しない。⚠️項目を足す日は、壊して**鳴ること**を確かめてから通す。
  📌なぜ機械に数えさせるか＝**日付の順が崩れても、号数が古くなっても、読んでいるだけでは
  気づけない**から。実際、この帳面を建てた日に蔵人が両方やらかした。

## 免責の書き方
どのページにも同じ趣旨を置いてあります。文言を変えるときは全ページ揃えること。
- まだ**開発中**であること
- ファイルを扱うときは**必ず控え（バックアップ）を取ってから**。上書きしたものは元に戻せない
- 使ったことで起きた損害まではお引き受けできない
- おかしいところがあればメールをください。ひとつずつ直します

## 触り方の作法
- **見えるところは博喜さんに相談**。文言・並び・色は施主の持ち物。
- **危険な操作（push＝公開 / 削除）は必ず確認**。`git commit`（ローカル）は蔵人が可、**push は博喜さんの指**。
  ⚠️ここは**pushした瞬間に世界へ出る**（GitHub Pages）。他のリポジトリより一段慎重に。
- 手を入れたら、実際にブラウザで開いて確かめる。**特にiPhoneの縦画面**。
- 見張りは三つ：**色や寸法**→`DESIGN検算.py`／**開発日記**→`nikki/検算.py`／**コマンド辞書**→`kad/jisho/検算.py`。
