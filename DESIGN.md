# DESIGN.md — 縁側屋の見た目の正典

このファイルは **色・字・寸法の「正」** です。2026-09-07 新設。
`CLAUDE.md` が「なぜ、そう作るか」を持ち、この一枚が **「では、いくつなのか」** を持ちます。

⚠️**このリポジトリは公開（public）です。** この一枚も `https://engawaya.com/DESIGN.md` として
誰でも読めます。書けないことは書かない。

---

## なぜ、この一枚が要るのか

憲法①は「外から何も読み込まない。**全部そのファイルの中に書く**」です。
つまり縁側屋は、**共通のCSSファイルを持てない家**です。棟が一つ増えるたびに、
色の表がもう一枚まるごと写されます。

写しが増えれば必ず食い違う——この家が何度も踏んだ病です。
実際、この一枚を建てた日に機械で測ったら、こうでした：

- 素の青いリンク（`#0000EE`）が **六つの棟に残っていた**。夜の地に沈んで、比 1.34〜1.96。
  いちばん重かったのは `oto/`——**目の見えない方・見えにくい方のための入り口**の、
  問い合わせ先のメールアドレスでした。
- `kad/tebiki/` の34枚は、暗い側の指定を**先に**書いたせいで、その塊がまるごと死んでいた。
  用事の札は比 1.1（＝ほぼ見えない）。
- `kad/jisho/` には夜の顔が無く、家じゅうが暗くなる時刻に、そこだけ真っ白でした。

どれも「直した人が、他の棟を知らなかった」だけです。**知らないのが悪いのではなく、
知る場所が無いのが悪い。** ここが、その場所です。

⚠️**実行時に共有できない家だから、書くときに共有する。** それがこの一枚の役目です。
CSSファイルを作って読み込ませてはいけません（憲法①が崩れます）。
値はここから**書き写して**、各ファイルの `<style>` の中に置きます。

## 順番（正典 → 生成物）

1. 色や寸法を変えたくなったら、**先に DESIGN.md を直す**
2. そのうえで、その値を使っている棟の `<style>` へ写す
3. `python3 DESIGN検算.py` を通す。**[FAIL] が出たら commit しない**

⚠️**HTML だけ直して、この一枚を古いまま置かないこと。** その瞬間に、
正典ではなく、ただの古い memo になります。

---

## 1. 色

この家には、色の表が**三枚**あります。棟ごとにどれを使うかは 1-4 の表で決まっています。

### 1-1. 母屋の表（四つの顔）

`<html data-time="morning|day|evening|night">` で顔を変えます。
札を付けるのは各ページ末尾のJS（時刻で決める。**座った人には選ばせない**）。

<!-- 検算:色表:母屋 -->

| 変数 | 何の色か | 素（`:root`） | 朝 5-9時 | 昼 9-16時 | 夕 16-19時 | 夜 19-5時 |
|---|---|---|---|---|---|---|
| `--sky-top` | 空の上のほう | `#eaf2f8` | `#dbe9f4` | `#f2efe6` | `#f3cfa2` | `#141c2b` |
| `--sky-bottom` | 空の下（縁側に近い側） | `#fdfbf6` | `#fdfcf7` | `#fbf8f1` | `#fae7cd` | `#232c3d` |
| `--ink` | 本文の字 | `#2b2622` | — | — | — | `#efe9e0` |
| `--ink-soft` | 補足の字 | `#6b625a` | — | — | — | `#a9a49c` |
| `--accent` | 差し色（焼けた木の色） | `#9c5f2d` | — | — | `#9d5426` | `#e0a061` |
| `--card` | 座布団（カード）の面 | `#ffffffcc` | — | — | — | `#2b3446e6` |
| `--card-line` | 座布団のふち | `#d9cfc2` | — | — | — | `#45506a` |
| `--rule` | 罫（区切り線） | `#ded3c4` | — | — | — | `#3d4659` |
| `--wood-1` | 縁側の板・明るいほう | `#c69a6d` | — | — | — | `#6b5236` |
| `--wood-2` | 縁側の板・暗いほう | `#a97c50` | — | — | — | `#4d3a26` |

「—」は**その顔では変えない**（素のまま）という意味です。
⚠️`--card` と `--rule` は片方だけ使う棟が多く、両方持つ棟もあります。
無い変数を書き足す必要はありません。**使う変数だけ、この表と同じ値で**置いてください。

### 1-2. 選ばせる表（黒地／白地）

読むことに全部を賭けている棟の表です。**時刻では変わりません。座った人が選びます。**

<!-- 検算:色表:選 -->

| 変数 | 何の色か | 黒地（`:root`＝既定） | 白地（`html.shiroi`） |
|---|---|---|---|
| `--kami` | 紙（地） | `#0d1424` | `#ffffff` |
| `--ink` | 字 | `#f2f4fa` | `#1a1a1a` |
| `--ink-soft` | やわらかい字 | `#c3cbdc` | `#444444` |
| `--sen` | 線 | `#435070` | `#999999` |
| `--akari` | 灯り（見出しの下線・焦点の枠） | `#ffd166` | `#8a5a00` |
| `--botan` | 押す場所の面 | `#223050` | `#f0f0f0` |
| `--botan-sen` | 押す場所のふち | `#8296c2` | `#555555` |

**なぜ母屋と揃えないか**：ここは読み上げソフトと大きな字だけで全部つかえることが役目です。
「時刻で表情が変わる」ことより、**「いま選んでいる地の色が、勝手に変わらない」** ほうが大事。
母屋と逆の作法ですが、理由があって逆にしてあります。
**既定は黒地**——まぶしさがつらい方が多いためです。

### 1-3. 遊び場の表（夜だけ）

星を見る場所なので、時刻によらず暗いままです。

<!-- 検算:色表:遊 -->

| 変数 | 何の色か | 値 |
|---|---|---|
| `--sora-ue` | 空の上 | `#0d1424` |
| `--sora-shita` | 空の下 | `#1a2338` |
| `--ink` | 字 | `#f2f4fa` |
| `--ink-soft` | 補足の字 | `#aab3c8` |
| `--akari` | 灯り・焦点の枠 | `#ffd166` |
| `--wakusen` | 枠の線 | `#435070` |
| `--waku-akari` | 枠の光るほう | `#7d90ba` |

⚠️**遊びの絵の色（スイカの赤、れんがの色、星の色）は、この表の外**です。
一つの棟でしか使わない色を持ち込むと、この表が「全部の色の一覧」になって、誰も読まなくなります。

### 1-4. どの棟が、どの表を使うか

<!-- 検算:棟割り -->

| 表 | 棟 |
|---|---|
| 母屋 | `index.html` `naze/` `nikki/` `koe/` `meter/` `michi/` `akapen/` `ochigee/` `ochigee/privacy/` `kad/` `kad/jisho/` |
| 選ばせる | `oto/` `hanashiaite/` `asobiba/narabe/` `indexplayland.html` |
| 遊び場 | `asobiba/` `asobiba/suikawari/` `asobiba/renga/` |
| 手引き | `kad/tebiki/` とその全ページ（`prefers-color-scheme` で二枚） |

`kad/tebiki/` だけは時刻の札ではなく、**閲覧機の設定**（`prefers-color-scheme`）で明暗を切り替えます。
34枚を配る作りで、時刻を測るJSを34回写したくなかったためです。

⚠️**この作法をとる棟では、暗い側の塊を、明るい側の指定より「あと」に書くこと。**
先に書くと、あとの規則に上書きされて塊ごと死にます。
（2026-09-07 まで、実際に34枚とも死んでいました。`DESIGN検算.py` の六）が見張ります）

### 1-5. 表と違えている棟（承知のうえの食い違い）

<!-- 検算:例外 -->

| 棟 | 変数 | 表 | 実物 | なぜ | いつ |
|---|---|---|---|---|---|
| `meter/` | `--card`（素） | `#ffffffcc` | `#ffffffdd` | 計算の紙。数字を読ませる面なので、少し濃く | 2026-09-07 記録 |
| `meter/` | `--card`（夜） | `#2b3446e6` | `#2b3446ee` | 同上 | 2026-09-07 記録 |
| `oto/` | `--kami`（黒地） | `#0d1424` | `#10151f` | この棟だけ先に建った。揃えるかは施主の判断 | 2026-09-07 記録 |
| `oto/` | `--ink`（黒地） | `#f2f4fa` | `#f5f7fb` | 同上 | 2026-09-07 記録 |
| `oto/` | `--sen`（黒地） | `#435070` | `#4a5670` | 同上 | 2026-09-07 記録 |
| `oto/` | `--botan`（黒地） | `#223050` | `#2a3855` | 同上 | 2026-09-07 記録 |
| `oto/` | 変数の名 | `--ink-soft` | `--ink-yawaraka` | 同上。**名前だけの違い**。値は表と同じ役目 | 2026-09-07 記録 |
| `indexplayland.html` | `--ink-soft`（白地） | `#444444` | `#3d3d3d` | 差はごく小さい。どちらも床の上 | 2026-09-07 記録 |
| `asobiba/` | `--ink` | `#f2f4fa` | `#eceef5` | 親だけ古い。子（`renga/` `suikawari/`）が新しい | 2026-09-07 記録 |
| `asobiba/` | `--ink-soft` | `#aab3c8` | `#9aa3b8` | 同上 | 2026-09-07 記録 |
| `asobiba/` | 変数の名 | `--wakusen` | `--waku` | 同上。旧い名前 | 2026-09-07 記録 |
| `asobiba/` | `--waku-akari` | `#7d90ba` | `#6f83ad` | 同上 | 2026-09-07 記録 |
| `asobiba/suikawari/` | `--sora-ue` `--sora-shita` | `#0d1424` `#1a2338` | `#101a2e` `#1c2942` | 夏の夜の畑。この遊びだけ空を明るく | 2026-09-07 記録 |

⚠️**ここは「直さない言い訳」を置く場所ではありません。** 載せてよいのは
**理由と日付のあるもの**だけ。理由が書けないものは、食い違いではなく、ただの直し忘れです。
📌上のうち `oto/` と `asobiba/` の分は、どれも読めることの床は越えています（＝害は出ていない）。
**揃えるかどうかは見た目の話＝施主の持ち物**なので、こちらでは動かしていません。

### 1-6. まだ抜けていない絵文字（憲法②の宿題）

憲法②は「絵文字を使わない。画面に出る小さな絵は、点をひとつずつ打って描く」です。
2026-09-07 に機械で数えたら、四つの絵が残っていました。二つはその日のうちに点で描き直し、
二つは**よその棟と写しがずれる**ので残しています。

<!-- 検算:憲法例外 -->

| 棟 | 絵 | なぜ、まだ直していないか | いつ |
|---|---|---|---|
| `kad/tebiki/` | ⚠️ 🔑 | 手引きの文は、**K'Ad蔵人の中の教本と同じもの**（非公開の `GCad` が正）。ここだけ直すと写しがずれる。**教本のほうと一緒に直す** | 2026-09-07 |

📌`michi/` の ✋ ✏️（「地図を動かす」「なぞって道」の札）は、**2026-09-07 に点で描き直しました**
（博喜さんのご判断）。1マス＝1ドットの矩形を並べた `<symbol>` を本文の頭に一度だけ置いて、
押す場所からは `<use>` で呼びます——同じ絵を二度書くと、片方だけ直る日が来るからです。
色は `currentColor` に預けたので、時間帯で字の色が変われば絵も一緒に変わります
＝**この絵のための色を、上の色の表に一つも足していません**。読み上げソフトには渡しません
（`aria-hidden`）。押す場所の言葉が本体で、絵は添えものだからです。
⚠️JSの中の短い言葉（地図の上の札と、警告の窓）は**絵を描けない場所**なので、
文言はそのままに、絵文字だけ落としました。

📌矢印（← → ↑ ↓）・丸数字（① ②）・図形（□ ● ▼）・鍵の字（⌘ ⌥ ⇧）・チェック（✓ ✕）は
**通します**。憲法②が止めたいのは「見る機械によって絵柄が変わるもの」だけで、
これらはどこで見ても同じ形の**字**だからです。実際この家は「→」を43枚で使っています。
そこまで鳴らす見張りは、すぐ切られて、無いのと同じになります。

---

## 2. 読めることの床（コントラスト）

**本文の字と、その下の地の比が 4.5 以上。**
大きい字（24px 以上、または 19px 以上の太字）は 3.0 以上。
線や飾りには床を課しません（読む対象ではないので）。

この床は好みではなく、**この家が助けたい相手から決まっています**。
見えにくい方が最初のお客さんの棟がいくつもあり（`oto/` `renga/` `meter/`）、
薄い字は「読みにくい」ではなく **「無い」** のと同じです。

### まだ床に届いていない組（宿題）

<!-- 検算:宿題 -->

| 組 | 実測 | いつ測ったか | どうするか |
|---|---|---|---|

**いまは、ありません。** 家じゅうの本文リンクが床（4.5）の上にあります。

⚠️**この表は、増やしてよい場所ではありません。** 新しく床を割る組を作ったら、それは欠陥です。
ここに載せてよいのは「**施主の判断を待っている**」ものだけ。理由と日付を必ず書くこと。

📌**差し色を濃くした経緯**（2026-09-07）。
もとの差し色 `#b4703a` は 朝 3.84／昼 3.72／夕（`#a95c2a`）4.09 で、四つの顔のうち三つが
床に届いていませんでした。**色相はそのまま、明るさだけ落として** `#9c5f2d`（夕は `#9d5426`）へ。
朝 5.00／昼 4.84／座布団の上 5.09／夕 4.66。夜の `#e0a061` は 9.37 なので、そのままです。
ぎりぎり越える最小の変更（`#a2632f`＝昼 4.54）ではなく**少し余裕のあるほう**を選びました。
——ちょうど床の上に置くと、**少し明るい地の上に載せた日に、また割る**からです。
⚠️この判断は博喜さんに伺って決めました。**差し色は、この家の顔です。**

---

## 3. リンクの作法

1. **どのページにも、リンクの色を決める規則を必ず置く。**
   置き忘れると閲覧機の既定の青 `#0000EE` が出ます。夜の地では比 1.5 前後＝**沈んで消えます**。
   ```css
   a { color: var(--accent); }   /* 母屋の表を使う棟 */
   a { color: var(--akari); }    /* 選ばせる表・遊び場の表を使う棟 */
   ```
   ⚠️`footer a { }` や `section a { }` だけでは足りません。**免責の段落の中のメールアドレスが
   素のまま残ります**（2026-09-07、実際に六つの棟でそうなっていました）。
   「どこそこの `a`」で数え上げると、**必ず数え落とします。** 総括の `a { }` を一つ置くこと。
2. **下線を消さない。** 色だけで「押せる」を伝えないためです。
   色の見え方は人によって違うので、色は二つ目の手がかりに留めます。
   ⚠️座布団のように「面まるごとが押す場所」は例外（下線は要りません。面が手がかりです）。
3. **焦点の枠を消さない。** `outline: none` を書くときは、必ず代わりの見え方を与えること。
   この家の作法は `outline: 3px solid var(--accent)` ＋ `outline-offset: 2px`、
   選ばせる表・遊び場の表の棟は `outline: 3〜4px solid var(--akari)`。

---

## 4. 字

| どこ | 大きさ | 行間 |
|---|---|---|
| 本文 | `clamp(15px, 3.8vw, 16.5px)` | `1.9` |
| 補足・注記 | `clamp(13px, 3.4vw, 14px)` | `1.7` |
| 小見出し | `clamp(18px, 4.5vw, 21px)` | `1.5` |
| 見出し | `clamp(28px, 6.5vw, 38px)` | `1.5` |
| 大見出し（母屋の看板） | `clamp(38px, 9vw, 56px)` | `1.5` |

選ばせる表の棟（`oto/` など）だけは行間 `2` ——読み上げと拡大が主役なので、ゆったり取ります。

書体（これで固定。**外から一つも読み込まない**＝憲法①）：

```css
font-family: "Hiragino Sans", "Hiragino Kaku Gothic ProN", "Yu Gothic Medium",
             "Yu Gothic", Meiryo, "Noto Sans JP", sans-serif;
```

数字を等幅で並べたいところ（`meter/` の計算の途中式など）だけ：

```css
font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
```

📌`"Noto Sans JP"` は**読み込みません**。入っている機械でだけ使われる、ただの候補です。
📌`kad/jisho/` は `"BIZ UDPGothic"` を候補に入れています（UDフォント＝読み違えにくい書体）。
他の棟へ広げるかは未決。**広げるなら全棟いっぺんに**（半分だけ違う家にしない）。

---

## 5. 押す場所（憲法③）

- **最小 44×44px。** ただしこの家の既定は **56px**、主役は **72px** です。
- 44px は「指で押せる」の下限で、**手が震える人には足りません**。
  だから下限ではなく、既定を大きく取っています。
- 押す場所どうしの間は **8px 以上**空ける（隣を巻き込まないため）。
- 面がそのまま押す場所になるもの（座布団）は、内側の余白 `24px 26px` を目安に。

| 使うところ | 高さ |
|---|---|
| 主役（母屋の座布団・音の縁側の押しろ） | `72px`（大きいもので `84〜96px`） |
| ふつうの押す場所 | `56px`（`58px` も可） |
| 添えの押す場所（小座布団・札） | `48px`（**これより下げない**） |

---

## 6. 面・線・影

| もの | 値 |
|---|---|
| 角の丸み・座布団 | `14px` |
| 角の丸み・小座布団や札 | `10〜12px` |
| 角の丸み・小さな部品 | `4〜8px` |
| 角の丸み・丸い札 | `999px` |
| 影・座布団（浮いたとき） | `0 10px 26px #0000001f` |
| 影・小座布団（浮いたとき） | `0 8px 20px #00000017` |
| 影・図の台 | `0 1px 4px rgba(0,0,0,.12)` |
| 線 | `1px solid var(--card-line)` または `var(--rule)` |

⚠️影は**昼の顔で決めて、夜で見直す**。夜はランプの光が回って、白い面が一つの山に溶けます
（2026-09-03、月見団子で実際に溶けました）。**昼に見て「少し濃いかな」でちょうどよい**。

---

## 7. 動き

- 色の移り変わりは `transition: background 1.2s ease, color 1.2s ease`（母屋）。
- 動きを減らす設定の人には、**入れ替えない**。
  ```css
  @media (prefers-reduced-motion: reduce) { /* 動かさず、一つだけ置く */ }
  ```
- 絵は点を打って描く（憲法②）。**絵文字は使わない。**

---

## 8. 測り方

見た目の話は、目で見るだけでは決めません。**機械に測らせます。**
手元で配信して（`.claude/launch.json` の `engawaya`）、ブラウザに計算させるのが早いです。

⚠️**測る前に `transition` を切ること。**

```js
const s = document.createElement('style');
s.textContent = '*,*::before,*::after{transition:none !important}';
document.head.appendChild(s);
document.documentElement.setAttribute('data-time', 'night');
```

📌これを書いているのは、2026-09-07 に**実際に間違えたから**です。
`transition: color 1.2s` の途中の値を読んで、「母屋の座布団のリンクが夜に比 1.18 で見えない」
という**嘘の欠陥を13件**数えました。切って測り直したら、四件とも問題なし。
**測り方を間違えると、無い病を治しに行きます。**

📌もう一つ。時刻の札は**読み込んだあとに JS が付ける**ので、あとから付け替えたときだけ
`transition` が走ります。実際に夜に来た人の画面では走りません（最初からその色なので）。
だから**これは使う人の欠陥ではなく、測る側の落とし穴**でした。

---

## 9. ここに書かないこと

- **その棟だけの色**（`michi/` の道の線、`suikawari/` のスイカの赤、`nikki/` の三つの札）
- **文言**。何と書くかは施主の持ち物で、`CLAUDE.md` と各ページの板書が持ちます
- **作った理由**。それは `naze/` と `CLAUDE.md`。ここは「いくつか」だけ
- **いま配っている版の番号**。同じ数字を二箇所に置かない（この家が何度も踏んだ病）

---

## 10. `DESIGN検算.py` が見ている八項目

```bash
python3 DESIGN検算.py
```

1. **色表と実物が合っているか** — 上の表と、各ページの `:root`／顔ごとの宣言を突き合わせる
   （1-5 の例外表に理由つきで載っているものは [宿題] として数え、[FAIL] にしない）
2. **棟割りに載っているか** — 共通の色の表を使っているのに 1-4 に名前が無いページ
3. **リンクの色を決める総括の規則があるか** — 素の青（`#0000EE`）の予防
4. **持つべき暗い顔を持っているか** — 母屋の棟なら `data-time="night"`、手引きなら暗い側の塊
5. **焦点の枠を消しっぱなしにしていないか** — `outline:none` を書いた棟に、代わりがあるか
6. **暗い側の塊が、いちばんあとに置かれているか** — 手引きが踏んだカスケードの病
7. **憲法①** — 外から読み込んでいないか（`@import` `@font-face` `url(https:` 外部の `src`／`stylesheet`）
8. **憲法②** — 画面に出るところに絵文字が無いか

⚠️この見張りは、建てた日に**八項目を一つずつ壊して、ぜんぶ鳴ることを確かめてあります**。
項目を足す日も、同じように鳴らしてから通してください。**鳴らない見張りは、無いのと同じです。**

📌**なぜ機械に数えさせるか**＝読んでいるだけでは気づけないからです。
素の青いリンクは六つの棟に、三十四日以上ありました。誰も嘘をついていません。
ただ、**人の目は「そこに規則が無いこと」を見つけられない**だけです。
